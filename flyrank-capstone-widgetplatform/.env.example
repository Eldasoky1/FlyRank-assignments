# --- Widget & lead-capture platform configuration ---
# Never commit real keys. Copy to `.env` and fill in.
MASTER_API_KEY=change-me-strong-key
ADMIN_USER=admin
ADMIN_PASSWORD=change-me

# Database file (omit for in-memory tests)
DATABASE_PATH=widgets.db
