# Evidence

For reviewer visibility, add the two screenshots described by FL-01 here and keep them
committed to this folder. Both stay private to logged-in reviewers via the submission
notes until they are added.

## 1. Configured Claude Project (screenshot)

- Open Claude → Projects → the project named `ai-fluency` (created with the custom
  instructions in `../claude-project-instructions.md`).
- Capture the Project info / custom instructions screen and save as
  `claude-project-screenshot.png` in this folder.
- Drop any edits into `evidence/` and push; the submission links already point at this
  directory, so no resubmission is required once the screenshot lands.

## 2. Anthropic Academy enrollment

- Enrolled in *AI Fluency: Framework & Foundations* at
  <https://anthropic.skilljar.com/ai-fluency-framework-foundations> with the same email as
  the portal account.
- Add `academy-enrollment.png` (course page showing first lesson completed) to this folder
  when convenient.